kind: pipeline
type: docker
name: default

steps:
- name: build
  image: mxnetci/build.ubuntu_cpu_python
  commands:
  - pip3 install pyyaml
  - ./dev_menu.py 13
  - echo done

trigger:
  branch:
  - master
